interface:
  display_name: "Agent Ready"
  short_description: "Repo-specific patterns and workflows for agent-ready"
  default_prompt: "Use the agent-ready repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true