{
  "name": "pipeline-ui-playwright",
  "version": "1.0.0",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  }
}
